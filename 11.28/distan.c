#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a;
    int arr[100];
    int i = 0;
    while (a != 0)
    {
        scanf("%d", &a);
        int p = a % 10;
        a = a / 10;
        arr[i];
    }
    return 0;
}